/*
 * AddEventForm Messages
 *
 * This contains all the text for the AddEventForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddEventForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddEventForm container!',
  },
});
