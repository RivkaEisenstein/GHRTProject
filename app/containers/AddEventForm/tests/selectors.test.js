// import { selectAddEventFormDomain } from '../selectors';

describe('selectAddEventFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
